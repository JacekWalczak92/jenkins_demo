pipeline {
  agent any
  
  stages {
    stage("build") {
    
      steps {
         echo 'build the app'
      }
    }
    stage("test") {
    
      steps {
        echo 'testig...'
      }
    }
    stage("deploy") {
    
      steps {
        echo 'deploy the app'
      }
    }
  }
}
